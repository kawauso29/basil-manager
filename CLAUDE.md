# basil-manager 開発ガイド（CLAUDE.md）

このファイルは Claude Code 用のガイドです。Codex 用には同じ内容を
`AGENTS.md` に用意しています。

> **重要:** Codex と Claude Code は、ガイドファイルを1つに共有する仕組みを
> 持っていません（`AGENTS.md` と `CLAUDE.md` はそれぞれ別ファイルとして
> 読み込まれます）。そのため、このファイルの内容を変更したときは、
> **必ず `AGENTS.md` にも同じ変更を反映してください**（逆も同様）。
> 2つのファイルの内容は常に完全に一致させること。

---

## 聖典（絶対原則）

以下は、このプロジェクトにおける最上位のルールです。他のどんな指示・
慣習よりも優先します。矛盾する指示があった場合は、作業を止めて
原田航希に確認してください。

### 1. このシステムの位置づけ

このバジル生産管理システムは、**原田航希個人**が自身のバジル生産
プロジェクトのためだけに使うものです。管理画面はソースコードとしては
一般公開していますが、実際に `/admin` を使うのは基本的に原田航希
本人のみです。

したがって、企業システムやマルチテナントSaaSを想定したような
**過剰なリスク想定・過剰なセキュリティ対策は今は不要**です。
不必要に複雑な認可設計、監査ログ、権限分離などを先回りして
実装しないでください。

### 2. コアクリスタルは原田航希が管理する

ER図・データモデル・アーキテクチャといった、システムの根幹
（コアクリスタル）は **原田航希自身が設計・実装を担当**します。
設計者であり使用者でもある原田航希が核となる部分を担当することで、
初めてこのシステムは長期的に維持可能になります。根幹の設計をAIに
委ねてしまうと、長期的にはシステムが崩壊します。

- 根幹の設計（テーブル構造、ER、主要なドメインモデルの責務分割など）に
  関与する実装を行う場合は、**必ず事前に原田航希に確認し、承認を得て
  から**着手すること。
- 確認なしに、根幹に関わる設計判断を先回りして実装しないこと。
- 迷ったら「これは根幹（コアクリスタル）に触れるか？」を自問し、
  少しでも該当しそうなら先に確認する。

### 3. YAGNI（You Aren't Gonna Need It）

過剰な機能追加は、複雑さを追加するだけです。使用者は原田航希のみ
なので、「将来使うかもしれない」機能・カラム・抽象化を先回りして
実装しないこと。

- 複雑なアクションやカラム追加を行うくらいなら、**手動運用を提案する**
  ことを優先する。
- 過度なシステム化・自動化は避ける。

### 4. KISS（Keep It Simple, Stupid）— 最重要

**KISSはこのプロジェクトで最も重要な原則です。**

アーキテクチャ、コード、その全てが徹底的にわかりやすく、
シンプルであること。専門用語を知らない人が読んでも流れを
追えるくらい、**幼児でも分かるレベルで透明**にしておくこと。

- 賢さよりも分かりやすさを優先する。
- トリッキーな書き方、過度な抽象化、過度なメタプログラミングは避ける。
- 迷ったら、より単純な方を選ぶ。

### 5. テスト・ドキュメントの追加は慎重に

テスト・ドキュメントは重要です。しかし、**管理できていないテスト・
ドキュメントには信頼性が付与されない**ことを理解すること。

AIによるテスト・ドキュメント作成は簡易で、放っておくと大量に生まれて
しまいます。しかしテスト・ドキュメントが増えるほど、「それは本当に
正しいのか？」という検証コストと、維持し続ける管理コストが大きく
なることを理解しなければなりません。

- テスト・ドキュメントの追加には慎重になること。「作れるから作る」の
  ではなく、本当に必要かをそのつど判断する。
- 追加したテスト・ドキュメントは、原田航希が継続してメンテナンスできる
  量・粒度に留める。
- 既存のテスト・ドキュメントを増やす前に、本当に価値があるか、
  すでにある内容と重複していないかを確認する。

---

## コメントの方針

モデル・コントローラでコメントが不足している場合は追記すること。
ただし、以下の基準に従う。

- 間違っている・古くなったコメントは、無いよりも害になる。内容が正しいか
  自信のないコメントは書かない。
- 一方で、**コメントが無いよりはあるほうが良い**。「ある場合」と
  「ない場合」を比べて、より分かりやすいほうを選ぶこと。
- Railsの標準的なCRUDアクション（index / show / new / create / edit /
  update / destroy）など、名前だけで意図が明確なものにコメントは不要。
- 業務ルールを表す定数、工程遷移、バリデーションなど、名前だけでは
  意図が伝わりにくい箇所には、簡潔な日本語コメントを付けること。

---

## HTML記述の方針

タグの改行・インデントは必ず行うこと。ただしインライン要素の場合は
必ずしも改行は必須ではない。ブロック要素の場合は改行とインデントを
必ず行うこと。

---

## AIエージェントの探索方針（コンテキスト効率）

タスクの着手前に、リポジトリ全体を読み込もうとしないこと。読み込む
ファイルが増えるほど、AIエージェントの入力トークン消費は際限なく
膨らむ。

- まず `rg`（または `rg --files`）で、タスクに関係しそうなモデル・
  コントローラ・ファイルを検索し、関係する範囲を絞り込むこと。
- 関連が確認できたファイルだけを読むこと。関連の薄いディレクトリを
  予防的に読み込まないこと。
- リポジトリの「地図」は既存の資産で足りている。新たに要約用の
  ドキュメント（`docs/ai/` のようなもの）を作らないこと。
  - 技術構成・主なモデルの概要: このファイルの「プロジェクト概要」
  - ドメイン・テーブル仕様・ER図: `doc/`（構成は `doc/AGENTS.md` を参照）
  - コード自体の構造: `rg --files` や `app/` 以下のディレクトリ構成
- 上記だけで判断がつかない場合に限り、探索範囲を一段階ずつ広げること。

---

## プロジェクト概要

バジルなどの植物生産管理を行う、原田航希個人用のRailsアプリケーション
です。種まき・挿し木から鉢上げ、個体管理、販売可能日の設定、栽培完了
までの一連の工程を、管理画面（`/admin`）から記録・追跡します。

主なモデル:

- **Plant / Location**: 植物の品種と、株を保管・栽培する場所
- **ProductionLot**: 種まき・挿し木による生産の出自
- **NurseryGroup**: 鉢上げ前の数量管理
- **Stock**: 鉢上げ後、または直接登録の個体（1株）
- **StockObservation**: 株の観察ログ（高さ・メモ・写真）

技術スタック: Ruby 3.4.10 / Rails 8.1 / PostgreSQL / Hotwire
（Turbo・Stimulus）/ Active Storage / Solid Queue・Cache・Cable / Kamal

詳細は [`README.md`](README.md) を参照してください。

## 開発コマンド

開発環境はDocker Composeを想定しています。

```bash
docker compose up
docker compose exec web bin/rails console
```

テスト（RSpec）:

```bash
docker compose exec web bash -lc "RAILS_ENV=test bundle exec rspec"
```

Lint / 静的解析:

```bash
docker compose exec web bin/rubocop
docker compose exec web bin/brakeman
```

実装を変更したら、完了とする前に上記のRSpecとrubocopを実行し、
通ることを確認すること。

## コミットメッセージの規約

先頭に種別（`feature` / `fix` / `refactor` / `update` / `docs` など）を
付け、続けて日本語で変更内容を簡潔に要約すること。

## ドキュメント運用

`doc/` ディレクトリに設計・仕様を記録しています。実装を変更する際は、
関連するドキュメントも同じ変更に含めてください。ドキュメントの構成と
更新ルールは [`doc/AGENTS.md`](doc/AGENTS.md) を参照してください
（`doc/db/<table>.md` のようなテーブル仕様書、`doc/er.md` のER図など）。
