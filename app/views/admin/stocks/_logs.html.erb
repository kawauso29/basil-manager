<% if logs.any? %>
  <ul class="log-list">
    <% logs.each do |log| %>
      <li>
        <span class="log-date"><%= log[:recorded_at] || "日時未設定" %></span>
        <span><strong><%= log[:label] %></strong> <%= log[:data_value] %></span>
        <span><%= image_tag(log[:image_path], class: "thumbnail", alt: "") if log[:image_path].present? %> <%= log[:memo].presence || "—" %></span>
      </li>
    <% end %>
  </ul>
<% else %>
  <p class="empty-state">記録はまだありません。</p>
<% end %>
