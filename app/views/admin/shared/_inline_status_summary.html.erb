<% if rows.any? %>
  <div class="summary-statuses">
    <% rows.each do |row| %>
      <span class="status status--<%= row[:key] %>">
        <%= row[:label] %> <%= row[:quantity] %>株
      </span>
    <% end %>
  </div>
<% else %>
  <span class="muted">—</span>
<% end %>
