<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title) || "印刷" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= stylesheet_link_tag "print", "data-turbo-track": "reload" %>
  </head>

  <body class="print-body">
    <%= yield %>
  </body>
</html>
